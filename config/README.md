# Configuration Files

Add the final Betaflight CLI backup here:

- `betaflight-diff-all.txt`

Also add screenshots of key setup pages to `media/images/` when they clarify board alignment, receiver setup, motor order, or flight modes.
